<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Formulário Dark</title>
    <style>
        body {
            background-color: #1c1c1c;
            color: #fff;
            font-family: Arial, sans-serif;
        }
        .container {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        label {
            display: block;
            margin-bottom: 10px;
            font-size: 18px;
        }
        #message {
            background-color: #333;
            border: none;
            border-radius: 5px;
            color: #fff;
            font-size: 16px;
            padding: 10px;
            margin-bottom: 20px;
            width: 100%;
            /*height: 200px;*/
        }
        input[type=submit] {
            background-color: #fff;
            border: none;
            border-radius: 5px;
            color: #333;
            cursor: pointer;
            font-size: 18px;
            padding: 10px 20px;
        }
        input[type=submit]:hover {
            background-color: #333;
            color: #fff;
        }
    </style>
</head>
<body>

  <div class="container">
      <h1 style="text-align:center">Test Disk</h1>
      <h4 style="text-align:center"><b>IP: </b><%= @ip %></h4>
      <h4 style="text-align:center"><b>Container id: </b><%= @container_id %></h4>

      <form method="post" action="/">
          <label for="message">Message:</label>
          <input id="message" name="message" required/>

          <div style="text-align:center">
              <input type="submit" value="Enviar"/>
          </div>
      </form>

      <% if @content && @content.size > 0 %>
        <hr>
        <% @content.split("\n").reverse.each_with_index do |message, index| %>
          <small>
            <%= index %> -
            <strong><%= message %></strong>
          </small>
          <br>
        <% end %>
        <hr>
      <% else %>
        <h3>Sem conteúdo no container</h3>
      <% end %>
  </div>

  <script>
      document.addEventListener('DOMContentLoaded', function() {
          document.querySelector('#message').focus()
      }, false);
  </script>
</body>
</html>
